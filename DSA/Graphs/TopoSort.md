# Topological Sort
## 25th June 2026
## Course Schedule 1 and 2
```js
vector<int> adj[n];
vector<int> indegrees(n);
if(indegrees[i]==0) // Push to the queue
while(!q.empty()){
    /// Push to the ans vector
    // Otherwise iterate its adjacency matrix, indegrees[v]--, if indegrees[v]==0 again Push it
}
```